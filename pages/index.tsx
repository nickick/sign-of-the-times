import { Box } from '@mui/material';
import type { NextPage } from 'next';
import Landing from '../src/Landing';

const Home: NextPage = () => (
  <Box
    sx={{
    }}
  >
    <Landing />
  </Box>
);

export default Home;
